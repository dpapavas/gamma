file(
  GENERATE
  OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/driver"
  CONTENT
"#!/bin/bash\n\
GAMMADB=\${GAMMADB:=$<TARGET_FILE:gammadb>} ${CMAKE_CURRENT_SOURCE_DIR}/driver \"$@\"\n"
  FILE_PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE
                   GROUP_READ GROUP_EXECUTE
                   WORLD_READ WORLD_EXECUTE
)
